import { Action }   from 'airflux';

/* Error Actions Manager */

export const boardKeyNoMatch       : Action = new Action().asFunction;
export const throwError            : Action = new Action().asFunction;
